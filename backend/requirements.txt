fastapi>=0.109.0
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.9
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0,<4.1
python-multipart>=0.0.6
httpx>=0.26.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.1.0
celery>=5.3.0
redis>=5.0.0
cryptography>=42.0.0
python-dotenv>=1.0.0
yt-dlp>=2024.3.0
# PoToken provider client: YouTube now rejects most format URLs (silently
# dropping them from the `formats` list, not just erroring) unless requests
# carry a valid proof-of-origin token. This plugin fetches one from the
# bgutil-provider sidecar container (see docker-compose.yml) over HTTP --
# without it, YouTube downloads fail with "Requested format is not
# available" even with valid cookies.
bgutil-ytdlp-pot-provider>=1.0.0
